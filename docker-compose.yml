

version: '3.8'

services:
  # MySQL 数据库
  mysql:
    image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/mysql:8.0.44
    container_name: iom-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-iom}
      TZ: Asia/Shanghai
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./migrations/init.sh:/docker-entrypoint-initdb.d/01-init.sh
      - ./migrations/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    networks:
      - iom-network

  # Redis 缓存
  redis:
    image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/redis:7-alpine
    container_name: iom-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - iom-network

  # 后端服务 (开发模式)
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iom-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-9876}:9876"
    environment:
      iom_SERVER_MODE: ${iom_SERVER_MODE:-debug}
      iom_SERVER_HTTP_PORT: 9876
      iom_SERVER_JWT_SECRET: ${iom_SERVER_JWT_SECRET:-your-secret-key-change-in-production}
      iom_DATABASE_HOST: mysql
      iom_DATABASE_PORT: 3306
      iom_DATABASE_DATABASE: ${MYSQL_DATABASE:-iom}
      iom_DATABASE_USERNAME: ${MYSQL_USERNAME:-root}
      iom_DATABASE_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
      iom_REDIS_HOST: redis
      iom_REDIS_PORT: 6379
      iom_REDIS_PASSWORD: ""
    depends_on:
      - mysql
      - redis
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    networks:
      - iom-network

  # 前端服务
  frontend:
    image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/node:18-alpine
    container_name: iom-frontend
    working_dir: /app
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    volumes:
      - ./web:/app
      - /app/node_modules
    environment:
      - VITE_API_BASE_URL=http://backend:9876
    depends_on:
      - backend
    networks:
      - iom-network

volumes:
  mysql-data:
    driver: local
  redis-data:
    driver: local

networks:
  iom-network:
    driver: bridge
